services:
  frontend:
    container_name: connect4_frontend
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    stdin_open: true   # 🔥 REQUIRED for Vite on Windows
    tty: true          # 🔥 REQUIRED for Vite on Windows
