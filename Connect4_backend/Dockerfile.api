# Use LTS Node
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy only backend package files first (cache friendly)
COPY package.json package-lock.json ./

# Install dependencies
RUN npm ci --only=production

# Copy backend source
COPY . .

# Expose backend port
EXPOSE 5000

# Start server
CMD ["node", "server.js"]
